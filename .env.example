# ─── AWS S3 ───────────────────────────────────────────────
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here
AWS_REGION=us-east-1
AWS_BUCKET_NAME=your-bucket-name

# ─── SUPABASE ─────────────────────────────────────────────
# Found in: Supabase Dashboard → Settings → API
SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
# Use Service Role key (server-side only, never expose to frontend)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ─── ADMIN SECURITY ───────────────────────────────────────
# Any random secret string — used to protect the upload endpoint
ADMIN_SECRET=make_up_a_strong_random_string_here

# ─── FRONTEND (exposed to Vite, note VITE_ prefix) ────────
# This is just the admin secret passed from the UI to the API header
VITE_ADMIN_SECRET=same_value_as_ADMIN_SECRET_above
